for(let i = 0; i < 100; i++){
    console.log(i)
} 
const prompt = requestAnimationFrame(`prompt-sync`)
console.log("entregando Noteboosk")
for(letbr + 1; nr <= 32; nr++){
    let nome = prompt(`quem e o numero ${nr}`)
    let presente = prompt(`O(A) ${nome} veio para a escola`)

    if (presente == 'S'){
        console.log(`pegar notbook ${nr}`);
        console.log(`pegar mouse ${nr}`);
        console.log(`pegar fonte ${nr}`);
        console.log(`levar o notebook ${nome}`);
    }else{
        console.log(`nao pegar o notbook ${nr}`);
    }
    }

